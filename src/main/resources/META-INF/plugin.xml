<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.mongoclientreusecompanion</id>
    <name>MongoDB Client Reuse Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning icon on a <code>new MongoClient(...)</code> (legacy
        driver) or <code>MongoClients.create(...)</code> (modern
        <code>com.mongodb.client</code> driver) construction written
        inside a regular method body -- MongoDB's own documentation
        states "you will only need one instance of class MongoClient
        even with multiple threads", and that not following the
        singleton pattern "may lead to too many open connections in
        MongoDB". A MongoClient holds an internal connection pool
        (default size 100) -- building one inside a regular method
        means a brand new pool on every call.</p>
        <p><b>100% static text/PSI analysis</b> -- matches the class
        name by simple text, so it works whether the real MongoDB
        driver jar is on the classpath or not. Java and Kotlin.</p>
        <p>Stated honestly: v0.1 only flags the "build from scratch"
        shape -- a client obtained by reference from an existing shared
        instance/dependency injection is never flagged (correctly,
        since it isn't the anti-pattern this plugin targets).</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends>org.jetbrains.kotlin</depends>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK1="true" supportsK2="true"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <codeInsight.lineMarkerProvider language="JAVA"
            implementationClass="dev.gaphunter.mongoclientreusecompanion.gutter.ClientBuiltPerCallLineMarkerProvider"/>
        <codeInsight.lineMarkerProvider language="kotlin"
            implementationClass="dev.gaphunter.mongoclientreusecompanion.gutter.ClientBuiltPerCallLineMarkerProvider"/>
        <notificationGroup id="MongoDB Client Reuse Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
